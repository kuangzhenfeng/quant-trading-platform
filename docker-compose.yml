version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - API_HOST=0.0.0.0
      - API_PORT=8000

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
