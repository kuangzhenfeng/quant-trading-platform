version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "9000:9000"
    environment:
      - API_HOST=0.0.0.0
      - API_PORT=9000

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
