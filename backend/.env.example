# === 账号批量导入（可选）===
# 启动时自动读取该 JSON 文件并导入账号，按 broker+name 去重，已存在则跳过
# ACCOUNTS_IMPORT_FILE=./accounts_import.json

# === 认证配置 ===
AUTH_ENABLED=true
AUTH_JWT_SECRET=your-secret-key-change-in-production
AUTH_JWT_ALGORITHM=HS256
AUTH_ACCESS_TOKEN_EXPIRE_DAYS=7

# === 数据库配置 ===
# 开发环境使用SQLite（无需安装PostgreSQL）
DATABASE_URL=sqlite+aiosqlite:///./quant_trading.db
# 生产环境使用PostgreSQL（需要先启动PostgreSQL服务）
# DATABASE_URL=postgresql+asyncpg://postgres:quant@localhost:5432/quant_trading
